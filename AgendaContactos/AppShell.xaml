<Shell
    x:Class="AgendaContactos.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:AgendaContactos.Pages">

    <FlyoutItem Title="Inicio">
        <ShellContent ContentTemplate="{DataTemplate pages:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Añadir contacto">
        <ShellContent ContentTemplate="{DataTemplate pages:AddContactPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Agenda">
        <ShellContent ContentTemplate="{DataTemplate pages:AgendaContactPage}" />
    </FlyoutItem>

</Shell>
